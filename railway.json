{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd sanad && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn sanad.wsgi",
    "runtime": "v2",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/"
  }
}